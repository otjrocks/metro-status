{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "DC Metro Status Configuration",
  "description": "Configuration for WMATA DC Metro real-time status display with live train arrivals",
  "properties": {
    "enabled": {
      "type": "boolean",
      "description": "Enable or disable the DC Metro Status plugin",
      "default": true
    },
    "wmata_api_key": {
      "type": "string",
      "title": "WMATA API Key",
      "description": "Your WMATA API key from https://developer.wmata.com/",
      "x-secret": true
    },
    "reference_station": {
      "type": "string",
      "title": "Reference Station",
      "description": "The metro station to display arrivals for",
      "default": "Metro Center",
      "examples": [
        "Courthouse",
        "Metro Center",
        "Gallery Place",
        "Union Station",
        "L'Enfant Plaza",
        "Farragut West",
        "Columbia Heights",
        "Silver Spring",
        "Vienna",
        "Ashburn"
      ],
      "ui:widget": "text"
    },
    "refresh_interval": {
      "type": "integer",
      "title": "Refresh Interval",
      "description": "How often to refresh train data in seconds (10-300)",
      "default": 30,
      "minimum": 10,
      "maximum": 300
    },
    "page_display_time": {
      "type": "integer",
      "title": "Page Display Time",
      "description": "How long to display each direction page in seconds (5-60)",
      "default": 10,
      "minimum": 5,
      "maximum": 60
    }
  },
  "required": [
    "enabled",
    "wmata_api_key",
    "reference_station"
  ],
  "x-propertyOrder": ["enabled", "wmata_api_key", "reference_station", "refresh_interval", "page_display_time"],
  "additionalProperties": false
}
